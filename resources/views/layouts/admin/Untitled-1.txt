$.ajax({
				type: 'get',
				url: url[0],
				data: 'abc',

				success:function(data) {
					console.log(data);
				},
				error: function()
				{
					alert('hi');
				},
		}).then(function(){
			setTimeout(update, 1000);

		})

         $.each(data, function(nilai, value){
                        if(value['nilai'] < 20)
                        alert('suhu tidak normal')
                    })

                                        $('#tabel_air').load(location.href + '#tabel_air');
